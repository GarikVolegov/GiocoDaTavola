# Ralph Progress Log
Started: 2026-06-20
---

## Codebase Patterns
- Stack: server/ (Node+Express+Socket.IO, TS) + client/ (React+Vite, TS), npm workspaces monorepo.
- Quality gate: `npm run typecheck && npm run lint && npm test && npm run build` from repo root.
- Server is authoritative; game state in-memory; votes are SECRET (only aggregate counts leave the server).
- Timers: server stores an expiry timestamp and broadcasts it; clients render the countdown locally.
- Server is CommonJS TS (module: CommonJS): use native __dirname, no .js import extensions needed. Client is ESM (Vite). Keep them separate.
- Quality gate is GREEN as of US-001: `npm run typecheck && npm run lint && npm test && npm run build`. Don't break it.
- ESLint flat config (eslint.config.js) uses typescript-eslint recommended (non type-checked) + no-unused-vars with ^_ ignore. Avoid `any` (no-explicit-any is an error).
- vitest config includes only `server/**/*.{test,spec}.ts` and has passWithNoTests. Put game-logic tests under server/src (e.g. server/src/game/__tests__).
- Client routing is path-based in App.tsx (`/host` -> HostApp, else PlayerApp); no react-router yet.
- Client socket: use `getSocket()` from client/src/shared/socket.ts — it's a memoized singleton, so StrictMode's double-mount and both views share ONE connection. Don't call io() directly.
- Shared client Socket.IO event names + payload types live in client/src/shared/events.ts (SocketEvents constants + payload interfaces), reused by host + player views. Server uses the same string literals in server/src/index.ts; keep them in sync.
- Server pattern for per-host state: map socket.id -> roomCode (hostRooms) so a re-emit returns the same room; clean up on 'disconnect'. Rooms themselves persist in the RoomStore for the process lifetime.
- Game logic modules go in server/src/game/*.ts with tests in server/src/game/__tests__/*.test.ts. Make randomness injectable (e.g. RoomStore(genCode)) so collisions/edge cases are testable deterministically.
- Adding a client dep: `npm install <pkg> --workspace client`. QR codes use `qrcode.react` (QRCodeSVG); wrap it in a white-background box for scannability in dark mode.
- `.last-branch` is a Ralph harness artifact; it's gitignored — don't commit it. `.vscode/` is also gitignored (editor config).
- Realtime broadcast: both host AND players `socket.join(code)`; broadcast aggregate state with `io.to(code).emit(event, payload)`. The lobby roster (nicknames) is PUBLIC — only VOTES are secret. A `broadcastLobby(code)` helper centralizes the emit.
- Player identity is the `socket.id` for now (US-015 reconnection will add a stable token). Player join dedups by socket.id like host rooms, so StrictMode's double-emit is harmless.
- Disconnect cleanup: keep a `playerRooms` map socket.id->code (mirrors `hostRooms`); on 'disconnect' remove the player and re-broadcast the lobby. Clean up BOTH maps.
- Client request/response over sockets: emit one event, server replies with a success event OR an error event (e.g. player:join -> player:joined | player:joinError). Map error CODES (not UI text) over the wire; keep a `JOIN_ERROR_MESSAGES` map in client/src/shared/events.ts for the Italian copy.
- Reading the QR join code: `new URLSearchParams(window.location.search).get('room')` prefills the player form; uppercase it (codes are A-Z).
- No browser tooling in this env: verify socket stories end-to-end with an ad-hoc `.mjs` script (import bare `socket.io-client` from repo root) run against the BUILT prod server (`PORT=xxxx node server/dist/index.js`). Delete the script after; note visual browser check as pending.
- Game phase lives on the Room (`phase: GamePhase`, `dilemmaCount`) and starts at 'LOBBY' in `create()`. The state machine is the server's job; clients only render the phase they receive. `GamePhase` is intentionally minimal ('LOBBY'|'PHASE_INTRO') and gets extended by US-005 — keep it in sync in BOTH server/src/game/rooms.ts and client/src/shared/events.ts.
- Authoritative game state broadcast: `broadcastGameState(code)` emits `game:state {phase, dilemmaCount}` to `io.to(code)` (host + all phones). host:createRoom also emits an initial game:state so a recovered host renders the right screen. US-005 timers should ride on this same event (add an expiry timestamp to the payload).
- Host-only commands: the host does NOT send the room code — look it up from `hostRooms.get(socket.id)` (the host owns exactly one room). Pattern: `host:startGame {dilemmaCount}` -> success broadcasts game:state, failure emits `host:startError {error}` to just that socket (mirror of player:join/joinError). Force-advance reuses this: `host:advancePhase` (no payload) -> advanceAndBroadcast.
- Constants shared by server + client are DUPLICATED by design (server is CJS rooms.ts, client is ESM events.ts; they can't import each other). MIN_PLAYERS_TO_START / DILEMMA_COUNT_OPTIONS / GamePhase / PHASE_LABELS exist in both — keep the values identical.
- State machine (US-005): pure `nextPhase(current,dilemmaIndex,dilemmaCount)->{phase,dilemmaIndex}` in rooms.ts is the single source of transition truth (DILEMMA_SEQUENCE = DILEMMA_REVEAL→VOTE_1→SPLIT_REVEAL→DEFENSE→VOTE_2→PHASE_RESULTS; PHASE_INTRO opens dilemma #1; PHASE_RESULTS loops to next DILEMMA_REVEAL while dilemmaIndex<dilemmaCount else FINAL_AWARDS). Keep it pure + unit-tested; later stories add per-phase data (votes, defenders) but should NOT change the order here.
- Server timers: durations live in `PHASE_DURATIONS_MS` (null = no timer: LOBBY waits for host, FINAL_AWARDS terminal). The Room stores `phaseExpiresAt` (epoch ms); `advancePhase`/`startGame` set it via an injectable clock (2nd RoomStore ctor arg `now()`), so transition+expiry are deterministically testable. Side-effecting setTimeout lives ONLY in index.ts (`phaseTimers` map + schedulePhase/clearPhaseTimer/advanceAndBroadcast), never in rooms.ts — keep the store pure.
- Timer wiring: schedulePhase(code) always clears the old timer first, so both expiry-fire AND host force-advance go through advanceAndBroadcast→schedulePhase and reschedule cleanly (no double-advance race). A phase with phaseExpiresAt==null ends the chain (no reschedule).
- game:state payload is the one authoritative broadcast: now `{phase, dilemmaCount, dilemmaIndex, phaseExpiresAt}` (build via `gameStatePayload(room)` helper, reused by broadcast + the host:createRoom recovery emit). Add future per-phase fields here.
- Client countdown: `useCountdown(expiresAt)` hook in client/src/shared/useCountdown.ts renders whole seconds from the server expiry (250ms tick, null when no timer). Reuse it on host + phones; the server is authoritative about WHEN — clients only render. Hooks must be called before the component's early returns.
- Deck (US-006): `server/src/game/deck.ts` — `loadDilemmas()` reads `server/data/dilemmas.json` via `join(__dirname,'..','..','data','dilemmas.json')` (resolves to server/data from BOTH server/src/game in dev and server/dist/game in prod; data is NOT copied into dist). `Dilemma = {id,text,optionA,optionB}`. `Deck` copies its source (never mutates), draws without repeats via injectable `rng` (default Math.random), exposes `remainingCount`, returns null when exhausted. The room owns a Deck per game and stores the drawn dilemma per round (US-007 wires it into DILEMMA_REVEAL + game:state).
- Per-round dilemma (US-007): the Room owns a `deck: Deck | null` + `currentDilemma: Dilemma | null`. `makeDeck` is a 3rd injectable RoomStore ctor arg (default `() => new Deck(loadDilemmas())`) — mirrors the genCode/now injection so tests pass a tiny deterministic deck. `startGame` creates `room.deck` and nulls currentDilemma; `advancePhase` draws (`room.deck.draw()`) ONLY when the transition lands on `DILEMMA_REVEAL`, so the same dilemma persists for the whole round (VOTE_1→…→PHASE_RESULTS) and each dilemma of the game is fresh (no-repeat). Surface non-secret per-round data through `gameStatePayload`/game:state (added `dilemma`); never put votes/identities there.
- Client TS gotcha: a non-null guard like `{game.dilemma && (...)}` does NOT survive inside a nested closure (e.g. `.map()` callback) for a MUTABLE object field — TS widens it back to nullable. Bind it to a local `const dilemma = game.dilemma` first, then reference `dilemma.x` everywhere; the const keeps the narrowing. (This bit the US-007 host option-map: `game.dilemma.optionA` inside `.map` → TS18047.)
- Shared payload growth: extend `GameStatePayload` in client/src/shared/events.ts AND build the matching field in the server `gameStatePayload(room)` helper together — the server emits an extra field silently, but the client only sees it once the type declares it (and typecheck guards the host render). Add a `PublicXxx` interface for each new public sub-object.
- Secret votes (US-008): the Room owns `votes: Map<playerId, VoteChoice>` ('A'|'B'), cleared on each DILEMMA_REVEAL (alongside the draw) so it persists VOTE_1→PHASE_RESULTS then resets per round. NEVER broadcast the map or the per-choice split — `gameStatePayload` exposes ONLY `votedCount` (room.votes.size); the A/B tally is revealed later (SPLIT_REVEAL, US-009). Keep the map present-players-only: `leave()` deletes the player's vote, and `vote()` rejects NOT_IN_ROOM, so `votes.size === players.size` ⇒ everyone voted. Aggregate readers on the store: `voteCount`/`voteTally`/`allVoted`. `vote()` overwrites (changeable until phase end). `isVotingPhase(phase)` (only VOTE_1 now; US-011 adds VOTE_2) gates both the store check and index.ts's disconnect/early-advance logic.
- Early phase advance: a player command can short-circuit the timer. Pattern in index.ts: after `rooms.vote(...)` ok, `rooms.allVoted(code) ? advanceAndBroadcast(code) : broadcastGameState(code)` — advanceAndBroadcast clears+reschedules the timer, so "everyone voted" and the 20s expiry share one path with no double-advance. Mirror it in `disconnect` (a leaving non-voter can complete the round).
- Player-private reply: confirm a player's own action back to JUST them with `socket.emit` (not `io.to(code)`) — e.g. `player:voted {choice}` echoes the recorded vote so a refused change reverts client-side. The phone tracks its selection in local state (optimistic on click; reconciled by player:voted / player:voteError), reset on `game.dilemmaIndex` change so each round starts unselected.
- Phase-gated public reveal (US-009): an aggregate that's secret during voting but public later gets a GATED reader on the store paired with a pure predicate — `isSplitRevealed(phase)` (only SPLIT_REVEAL) + `RoomStore.publicSplit(code)` returns voteTally only when revealed, else null. Keep voteTally (always computes, internal) separate from publicSplit (gated, broadcast). gameStatePayload surfaces `split: rooms.publicSplit(room.code)`. Gate to ONLY the reveal phase, NOT "SPLIT_REVEAL onward" — VOTE_2 (US-011) re-uses the votes map for a live re-vote, so revealing through it would leak a live split. Mirror this for any future "reveal once, hide while voting" aggregate.
- Host narrowing for a NEW mutable game:state field used in a closure: same gotcha as US-007's dilemma — bind `const split = game.split` next to `const dilemma = game.dilemma`, guard `phase === 'X' && split`, and reference `split.A` in the `.map`. Adding a nullable field to GameStatePayload that the host maps over will hit TS18047 unless bound to a local const.
- Intra-phase turn loop (US-010): a phase with multiple timed sub-turns (DEFENSE: one per defender) keeps `nextPhase` as the PHASE-level truth (DEFENSE->VOTE_2) and handles the turns in `advancePhase` BEFORE calling nextPhase: `if (phase==='DEFENSE' && turnIndex < defenders.length-1) { turnIndex++; phaseExpiresAt = expiryFor('DEFENSE'); return ok; }` — re-arms a fresh per-turn timer and returns without leaving the phase; only when turns are exhausted does it fall through to the normal transition. The index.ts timer/force-advance wiring (advanceAndBroadcast->schedulePhase reads phaseExpiresAt) needs NO change — each turn reschedules automatically, and host force-advance walks turn-by-turn. PHASE_DURATIONS_MS.DEFENSE is the PER-TURN duration, not the whole phase. Don't put per-turn state in nextPhase or the pure-transition tests stay valid unchanged.
- Defender selection (US-010): added `rng: () => number = Math.random` as the 4th injectable RoomStore ctor arg (after genCode/now/makeDeck) for deterministic picks in tests (`() => 0` = first voter, `() => 0.99` = last). `selectDefenders` reads the secret votes map, one defender per side (A before B), skips a side with 0 votes. The defenders' identities/side become public (inherent to speaking) — surface via a phase-gated `publicDefense(code)` reader (mirror of publicSplit + new `isDefensePhase` predicate) that returns {speaker:{id,nickname,side}|null, turn, totalTurns} ONLY in DEFENSE; NO other votes leak. The defender's OWN phone detects its turn by comparing `defense.speaker.id` to its player id — PlayerApp now stores its id from the player:joined payload (`player.id`) for this.
- Robust multi-step advance tests: when a story adds sub-turns to a phase, existing tests that hardcoded a fixed number of `advancePhase` calls to cross that phase will break. Fix them to LOOP on an observable (e.g. `while (room.dilemmaIndex !== 2) advancePhase`) instead of weakening the assertion — preserves the real intent and survives future phase changes.
- Second vote + swing (US-011): `isVotingPhase` now matches VOTE_1 OR VOTE_2 (gates `vote()`). The Room keeps the live current vote in `votes` AND a `votes1` snapshot taken when `advancePhase` enters VOTE_2 (`room.votes1 = new Map(room.votes)`); `votes` is LEFT INTACT on VOTE_2 entry, so each player's first vote is the changeable default and the live re-vote overwrites it. Clear BOTH on DILEMMA_REVEAL; `leave()` prunes BOTH. `computeSwing(code)` returns {first,second:{A,B}, switched, netSwing:{A,B}} comparing votes1 vs votes (switched = voters present in both whose side changed; netSwing = second-first per side; unknown room => zeros). A private static `RoomStore.tally(map)` helper is shared by `voteTally` + `computeSwing`.
- EARLY-ADVANCE is VOTE_1-only: because VOTE_2 starts pre-filled with defaults, `allVoted` is already true at entry — so the "everyone voted -> skip timer" short-circuit in index.ts (player:vote handler + disconnect) is gated to `room.phase === 'VOTE_1'`. VOTE_2 always runs its full timer (or host force-advance). Don't reuse `allVoted` to early-advance VOTE_2 or the first re-voter ends the phase for everyone. Keep `publicSplit` gated to SPLIT_REVEAL ONLY so the live VOTE_2 re-vote never leaks; swing is NOT broadcast yet (US-012 surfaces before->after in PHASE_RESULTS via its own gated reader).
- Phone re-vote default: the player view condition is `phase === 'VOTE_1' || phase === 'VOTE_2'`. The phone's local `vote` state persists across SPLIT_REVEAL/DEFENSE within a round (it only resets on `game.dilemmaIndex` change), so in VOTE_2 the first-vote button is already pre-selected as the default — no server round-trip needed to show it. A silent player keeps their default because `room.votes` already holds it server-side.
- CONCURRENCY HAZARD (observed US-011): a parallel agent/loop runs against the SAME working tree and commits with `git add -A`, which SWEEPS UP another in-progress story's uncommitted files. During US-011 the commit `dae1fde` ('feat: tag dilemmas by register...') grabbed this story's edited rooms.test.ts. Mitigation: commit your story with EXPLICIT paths (never `git add -A`), commit promptly once green, and don't stage `.claude/` (local harness settings + worktrees, untracked, not gitignored). It also means `npm test` can transiently fail on files you never touched (another story mid-flight) — re-run before assuming you broke something.
- Design system (Neon party night): the client has a DARK design system. Tokens are CSS custom properties in client/src/shared/ui/tokens.css (imported once in client/src/main.tsx BEFORE index.css); reusable themed components live in client/src/shared/ui/*.tsx with co-located *.module.css (CSS Modules, no deps) and a barrel index.ts. Available: Stage(variant 'host'|'player'), Button(variant 'primary'|'ghost', size 'md'|'lg'), Card(glow? 'a'|'b'|'accent'), OptionCard(faction 'a'|'b', letter, label), Pill(selected?, onClick? -> button w/ aria-pressed, else span), Countdown(seconds: number|null, pulses+danger <10s), CodeDisplay(code), Field(label)+TextInput(mono?), Alert(tone? 'danger', role=alert). WHEN BUILDING/EDITING VIEWS (HostApp/PlayerApp/new screens): compose these components and reference colors/spacing via var(--token) — do NOT add hardcoded hex or re-inline button/card/pill/input/countdown styling. Factions: A=blue (--faction-a), B=orange (--faction-b), accent=violet. Display font Space Grotesk is self-hosted (client/src/assets/fonts) — no runtime CDN. Respect prefers-reduced-motion (components already do). A few one-off LAYOUT inline styles (unique flex wrappers, min(..vw,..) widths, the QR white box) are fine if they reference tokens. No client test runner — verify client work with typecheck/lint/build. Full design tokens/components spec: docs/superpowers/specs/2026-06-20-design-system-design.md.
- BRAND: the game is named **SCHIERATI** ("il gioco dei dilemmi tra amici"; internal codename still "Dibattiti tra amici" in some historical docs). Logo assets (library) in brand/; the LIVE app assets are the 3 fixed-name files in client/public/ (schierati-logo.svg = host header, schierati-logo-transparent.svg = landing, schierati-icon.svg = favicon) — to swap the look, copy a brand/*.svg variant over the matching client/public/* file. Live variant = Space Grotesk (the app --font-display, base64-embedded so it renders inside an <img>) + faction colours (blu --faction-a / arancio --faction-b). Page title + favicon in client/index.html. Brand design doc: docs/superpowers/specs/2026-06-21-schierati-brand-design.md.
- RECONNECTION (phones survive lock/refresh/network blip): a player's PUBLIC id (in the roster, compared client-side for "tocca a te") is now decoupled from the socket id; a SEPARATE secret reconnect `token` (never broadcast — sent only to the joining socket in player:joined, stored in phone localStorage 'schierati:session') reclaims the seat. index.ts owns the session tables: sessions(socketId->{code,playerId}), tokens(token->{code,playerId}, survives disconnects), playerSocket(playerId->live socketId, so a lingering OLD socket doesn't evict a player who already reconnected), graceTimers(playerId, 45s removal). player:join accepts optional token; disconnect flags absent + schedules removal after RECONNECT_GRACE_MS; reconnect cancels it. rooms.ts: Player.connected?:boolean (optional, absent/true=present, false=absent — mirrors isBot?); setConnected() holds slot+vote; allVoted() counts only connected players (locked phone can't stall/block VOTE_1 early-advance). Verify index.ts socket flows with an ad-hoc .mjs socket script vs the BUILT server (no unit runner there) — delete after. PublicPlayer.connected surfaced; host+phone rosters dim absent players.
- rooms.ts MODULE SPLIT: the stateless phase state machine lives in game/phases.ts (GamePhase, PHASE_DURATIONS_MS, isVotingPhase/isSplitRevealed/isDefensePhase, pure nextPhase/nextDuelPhase, PhaseTransition); end-game scoring lives in game/awards.ts (PlayerStats, Award, ensureStats, pure computeAwards(room)). rooms.ts imports what it uses and `export * from './phases'` + `export type {...} from './awards'`, so tests/index.ts keep importing these from './rooms' UNCHANGED. RoomStore keeps thin computeAwards(code)/publicAwards(code) methods delegating to the pure fn. Duel logic (advanceDuelPhase etc.) is still in rooms.ts — it's `this`-coupled (rng/now/expiryFor), extract only with care.
- CONTENT: server/data/dilemmas.json now has 60 dilemmas, balanced 30 vita / 30 business (ids d01..d60). deck.test.ts enforces >=60 total, >=28 per register, distinct A/B options, unique texts — keep those invariants when editing content.
- PHONE UX: navigator.vibrate (guarded helper buzz() in PlayerApp, no-op where unsupported) — short tap on vote, triple buzz when it becomes your turn to speak (DEFENSE/DUEL_ARGUE). The defender/duel speaker also sees the dilemma text on their "tocca a te" screen. "Esci dalla stanza" clears the saved session.
---

## 2026-06-20 - US-001
- Implemented: TypeScript npm-workspaces monorepo scaffold.
- Files: package.json, eslint.config.js, vitest.config.ts, .gitignore; server/{package.json,tsconfig.json,src/index.ts,src/__tests__/smoke.test.ts}; client/{package.json,tsconfig.json,vite.config.ts,index.html,src/*}; README.md.
- Server: Express + Socket.IO on PORT (default 3000); /api/health; serves client/dist in prod (single port). Client: React+Vite, host/player views split by path.
- Verified: npm run typecheck/lint/test/build all pass; `PORT=3999 node server/dist/index.js` returns {"ok":true} on /api/health and serves built index.html.
- **Learnings for future iterations:**
  - Run `npm install` once before checks if node_modules missing.
  - `npm run dev` runs server (tsx watch, :3000) + client (vite, :5173) via concurrently; Vite proxies /socket.io (ws) and /api to :3000.
  - Next story US-002: room creation on /host with 4-letter code + QR. Add room store under server/src/game/rooms.ts and socket events; reuse client/src/shared/socket.ts.
---

## 2026-06-20 - US-002
- Implemented: host opens /host -> server creates an in-memory room with a unique 4-letter code; host shows the code large + a QR pointing to the join URL.
- Files: server/src/game/rooms.ts (RoomStore + generateRoomCode), server/src/game/__tests__/rooms.test.ts (5 tests, TDD), server/src/index.ts (host:createRoom/host:roomCreated handlers + per-socket dedup), client/src/shared/socket.ts (getSocket singleton), client/src/shared/events.ts (new shared event names/types), client/src/host/HostApp.tsx (code + QRCodeSVG), client/package.json (+qrcode.react), .gitignore (+.last-branch).
- Verified: typecheck + lint + test (6 passing) + build all green. Ran prod server (PORT=3999): /api/health ok, /host serves built client, socket host:createRoom returns a valid /^[A-Z]{4}$/ code and the same socket re-emitting gets the SAME code (StrictMode-safe).
- **Learnings for future iterations:**
  - React StrictMode double-invokes effects in dev -> a naive "create room on mount" makes 2 rooms. Solved server-side with a socket.id->code map (dedup) + the getSocket() singleton so the socket id stays stable. Apply the same idea to player join (US-003) to avoid duplicate joins.
  - QR join URL is `${window.location.origin}/?room=CODE`; US-003's join form should read the `room` query param to prefill the code.
  - socket.io-client is hoisted to the ROOT node_modules (npm workspaces). For ad-hoc node verification scripts, import the bare specifier `socket.io-client` from the repo root, not from client/node_modules.
  - Next story US-003: join form on / (code + nickname), realtime lobby player list, max 8, error on bad code. Add a players collection to Room and join/leave socket events; broadcast aggregate lobby state to host + all phones.
---

## 2026-06-20 - US-003
- Implemented: player joins from / with code + nickname and appears in a realtime lobby on host + all phones; bad code / empty nickname / full room are rejected with clear messages; max 8 players.
- Files: server/src/game/rooms.ts (Player/Room.players + MAX_PLAYERS + join/leave/listPlayers, JoinResult/JoinError), server/src/game/__tests__/rooms.test.ts (+8 tests, TDD), server/src/index.ts (player:join handler, host+player socket.join(code), broadcastLobby, playerRooms disconnect cleanup), client/src/shared/events.ts (new events + payload types + JOIN_ERROR_MESSAGES), client/src/host/HostApp.tsx (lobby roster), client/src/player/PlayerApp.tsx (join form + lobby view), .gitignore (+.vscode/).
- Verified: typecheck + lint + test (14 passing) + build all green. End-to-end socket round-trip against the built prod server (PORT=3998): 12/12 assertions — realtime roster to host + all phones, ROOM_NOT_FOUND on bad code, NICKNAME_REQUIRED on empty nick, fills to 8, 9th gets ROOM_FULL, disconnect removes the player from the lobby.
- **Learnings for future iterations:**
  - Lobby roster is PUBLIC by design (membership + nicknames); only VOTES are secret. broadcastLobby(code) emits aggregate `lobby:update {players}` to `io.to(code)`.
  - Both host AND players join the Socket.IO room (socket.join(code)) so io.to(code) reaches everyone; host:createRoom now also re-emits the current roster so a recovered host shows existing players.
  - Player identity = socket.id; join() dedups on it (re-join updates nickname) — StrictMode-safe and a stepping stone for US-015 reconnection (which should swap socket.id for a stable token + preserve scores/votes).
  - Next story US-004: host selects 3/4/5 dilemmas; Start enabled only with >=3 connected players; on start move LOBBY -> PHASE_INTRO. Will need a per-room phase/state field (start of the US-005 state machine) and a host:startGame event guarded by player count.
---

## 2026-06-20 - US-004
- Implemented: host picks 3/4/5 dilemmas and starts the game once >=3 players are present; the room state machine moves LOBBY -> PHASE_INTRO and host + phones re-render to the started screen.
- Files: server/src/game/rooms.ts (Room.phase/dilemmaCount, GamePhase, MIN_PLAYERS_TO_START, DILEMMA_COUNT_OPTIONS, startGame + StartGameError/Result), server/src/game/__tests__/rooms.test.ts (+7 TDD tests, 21 total), server/src/index.ts (host:startGame handler + broadcastGameState + initial game:state on host:createRoom), client/src/shared/events.ts (HostStartGame/HostStartError/GameState events, payload types, START_ERROR_MESSAGES, shared constants), client/src/host/HostApp.tsx (dilemma selector + Start button gated on >=3 + PHASE_INTRO view), client/src/player/PlayerApp.tsx (game:state listener + 'La partita è iniziata!' screen).
- Verified: typecheck + lint + test (21 passing) + build all green. End-to-end socket round-trip vs built prod server (PORT=3997): 10/10 assertions — LOBBY init, start blocked at 2 players, invalid count rejected, PHASE_INTRO broadcast to host + all phones with dilemmaCount, re-start blocked (ALREADY_STARTED). Visual browser check still pending real browser tooling.
- **Learnings for future iterations:**
  - Validation order in startGame: ROOM_NOT_FOUND -> ALREADY_STARTED (phase!=LOBBY) -> INVALID_DILEMMA_COUNT -> NOT_ENOUGH_PLAYERS. ALREADY_STARTED is checked before count so a valid re-start surfaces the right error and the original dilemmaCount is preserved.
  - `Number(payload?.dilemmaCount)` at the socket boundary + the `isDilemmaCount` type guard keep the wire input honest without `any`.
  - Next story US-005: full phase state machine (DILEMMA_REVEAL, VOTE_1, SPLIT_REVEAL, DEFENSE, VOTE_2, PHASE_RESULTS, FINAL_AWARDS) + server-side timers. Extend GamePhase in BOTH rooms.ts and events.ts; add an expiry timestamp to the game:state payload; auto-advance on expiry + a host force-advance (reuse the hostRooms.get(socket.id) host-command pattern); unit-test the transitions.
---

## 2026-06-20 - US-005
- Implemented: full phase state machine (LOBBY→PHASE_INTRO→DILEMMA_REVEAL→VOTE_1→SPLIT_REVEAL→DEFENSE→VOTE_2→PHASE_RESULTS→loop→FINAL_AWARDS) with authoritative server-side timers (expiry timestamp broadcast), auto-advance on expiry, and host force-advance. Host + phones render a server-driven countdown.
- Files: server/src/game/rooms.ts (extended GamePhase, PHASE_DURATIONS_MS, pure nextPhase + PhaseTransition, Room.dilemmaIndex/phaseExpiresAt, injectable now() clock, startGame timing, advancePhase + AdvancePhaseError/Result), server/src/game/__tests__/rooms.test.ts (+11 TDD tests, 32 total), server/src/index.ts (phaseTimers map, schedulePhase/clearPhaseTimer/advanceAndBroadcast, gameStatePayload helper, host:advancePhase handler, timer armed on host:startGame), client/src/shared/events.ts (GamePhase mirror, GameStatePayload +dilemmaIndex/phaseExpiresAt, PHASE_LABELS, HostAdvancePhase event), client/src/shared/useCountdown.ts (new hook), client/src/host/HostApp.tsx (in-game phase view: label + Dilemma X/N + countdown + 'Avanti' force-advance), client/src/player/PlayerApp.tsx (phase label + countdown).
- Verified: typecheck + lint + test (32 passing) + build all green. End-to-end socket round-trip vs built prod server (8/8): LOBBY no timer, PHASE_INTRO expiry ~5s ahead, auto-advance actually fired on the server timer (5003ms), force-advance walked the exact full sequence across 3 dilemmas to FINAL_AWARDS, FINAL_AWARDS terminal (force-advance no-op). Visual browser check still pending real browser tooling.
- **Learnings for future iterations:**
  - Keep the transition function PURE (nextPhase) and the timer side-effects (setTimeout) OUT of the store — inject a clock for deterministic expiry tests. This kept all 11 transition/timing tests synchronous and fast.
  - schedulePhase clears before scheduling, so timer-expiry and force-advance share one path (advanceAndBroadcast) with no double-advance race; a null phaseExpiresAt naturally ends the timer chain at FINAL_AWARDS.
  - Verification gotcha: when draining a socket-event queue, don't `await` an event that was already consumed (it hangs forever). Race the wait against a timeout to assert "no further event" (used for the FINAL_AWARDS terminal check).
  - Next story US-006: deck loading from server/data/dilemmas.json (>=20 items, text + option A + option B) + no-repeat draw within a game. Add a deck module under server/src/game/ with injectable RNG (mirror RoomStore's genCode pattern) and unit-test no-repeat; the room will need to hold the drawn dilemma per round (wire it into DILEMMA_REVEAL, surfaced via game:state for US-007).
---

## 2026-06-20 - US-006 (taken over from Ralph; built by hand)
- Implemented: dilemma deck module — load >=20 dilemmas from server/data/dilemmas.json and draw without repeats within a game.
- Files: server/src/game/deck.ts (loadDilemmas + Dilemma + Deck with injectable rng), server/data/dilemmas.json (24 dilemmas, unique ids, all fields non-empty), server/src/game/__tests__/deck.test.ts (9 tests — the test was scaffolded TDD-first by the prior loop; verified RED before writing deck.ts, then GREEN).
- Verified: deck test RED (no deck.ts) -> GREEN (9 passing); full gate `typecheck && lint && test (41 passing) && build` all green. Re-verified on take-over and additionally ran the COMPILED build (`node -e` against server/dist/game/deck.js): loadDilemmas found server/data/dilemmas.json at runtime and drew all 24 with no repeats, exhausting to null — empirically confirms the __dirname path resolves to server/data from dist too (not just tsx tests).
- **Learnings for future iterations:**
  - `loadDilemmas()` resolves data via `join(__dirname,'..','..','data','dilemmas.json')` — server/data is shared by dev (server/src/game) and prod (server/dist/game); the build does NOT copy data into dist, and that's fine because the relative path lands on server/data either way.
  - Deck.draw() splices a random index out of a private copy of the source (never mutates the caller's array) and returns null when exhausted; rng is injectable (default Math.random) for deterministic tests.
  - Next story US-007: reveal the dilemma on the host. The room should own a Deck for the game (draw on entry to each DILEMMA_REVEAL) and store the current dilemma; surface its text + optionA/optionB through the game:state broadcast (add an optional `dilemma` field) so the host renders it. Keep votes/identities out — this is just the prompt text.
---

## 2026-06-20 - US-007
- Implemented: the room draws a dilemma on entering DILEMMA_REVEAL and the shared host screen shows the dilemma text + option A / option B; PHASE_INTRO shows the phase label + short rules for ~5s before the first reveal. (Most server+client code was scaffolded by the prior loop but left UNCOMMITTED and with a failing client typecheck; I fixed it, verified the full gate + end-to-end, then committed.)
- Files: server/src/game/rooms.ts (Room.deck/currentDilemma, makeDeck injectable ctor arg, startGame seeds deck, advancePhase draws on DILEMMA_REVEAL), server/src/game/__tests__/rooms.test.ts (+4 TDD tests, 35 in file / 45 total), server/src/index.ts (gameStatePayload surfaces room.currentDilemma as `dilemma`), client/src/shared/events.ts (PublicDilemma + GameStatePayload.dilemma), client/src/host/HostApp.tsx (DILEMMA_REVEAL text + A/B cards; the TS-narrowing fix).
- Verified: typecheck + lint + test (45 passing) + build ALL green. End-to-end vs built prod server (PORT=3996, 15/15): real deck loaded, PHASE_INTRO has no dilemma, DILEMMA_REVEAL surfaces real text+optionA+optionB, same dilemma id persists through VOTE_1/SPLIT_REVEAL/DEFENSE/VOTE_2/PHASE_RESULTS, 2nd dilemma differs (no repeat). Visual browser check still pending real browser tooling.
- **Learnings for future iterations:**
  - A handed-over working tree can be ~90% done but RED — always run the FULL gate (typecheck especially) before trusting/committing leftover work. The bug here was a closure dropping a non-null narrowing on a mutable field (see Codebase Patterns: bind to a local const).
  - The dilemma intentionally persists for the whole round (drawn only on the DILEMMA_REVEAL transition, not re-drawn per phase), so later stories (VOTE_1, SPLIT_REVEAL, DEFENSE) can keep showing the prompt for context; the host renders `game.dilemma` whenever present, not gated to one phase.
  - Next story US-008: first SECRET vote from phones in VOTE_1 — each phone shows A/B and records one choice; host shows only the COUNT voted (e.g. 5/6), never who voted what; ~20s timer that also advances early once everyone has voted; vote changeable until expiry. Store votes Map<playerId, 'A'|'B'> on the Room (NEVER broadcast individual votes — only the aggregate voted-count), add a player:vote event, and let the server short-circuit the timer when all connected players have voted.
---

## 2026-06-20 - US-008
- Implemented: secret first vote in VOTE_1. Each phone shows A/B buttons and records one (changeable) choice; the host shows only the aggregate "Hanno votato X/Y"; the 20s timer auto-advances early once every connected player has voted. Individual votes never leave the server.
- Files: server/src/game/rooms.ts (Room.votes + VoteChoice + isVotingPhase/isVoteChoice, vote/voteCount/voteTally/allVoted, votes cleared on DILEMMA_REVEAL, leave() drops the vote, VoteError/VoteResult), server/src/game/__tests__/rooms.test.ts (+11 TDD tests, 46 in file / 56 total), server/src/index.ts (player:vote handler + early-advance, gameStatePayload.votedCount, disconnect recomputes during a voting phase), client/src/shared/events.ts (PlayerVote/PlayerVoted/PlayerVoteError events, VoteChoice + payloads, VOTE_ERROR_MESSAGES, GameStatePayload.votedCount), client/src/host/HostApp.tsx (VOTE_1 voted-count line), client/src/player/PlayerApp.tsx (VOTE_1 A/B voting view + local vote state).
- Verified: typecheck + lint + test (56 passing) + build ALL green. End-to-end vs built prod server (PORT=3995, 16/16): vote-before-VOTE_1 → NOT_VOTING_PHASE, invalid choice → INVALID_CHOICE, votedCount 0→1, change A→B keeps count 1, 2/3 stays VOTE_1, 3/3 auto-advances to SPLIT_REVEAL before the 20s timer, and the game:state keys are exactly {phase,dilemmaCount,dilemmaIndex,phaseExpiresAt,dilemma,votedCount} (no vote details leak). Visual browser check still pending real browser tooling.
- **Learnings for future iterations:**
  - Secrecy is structural: keep the votes Map server-side and surface ONLY votedCount via game:state; voteTally exists on the store (added now, used by US-009 SPLIT_REVEAL) but is NOT broadcast in VOTE_1.
  - Early advance + the timer share advanceAndBroadcast (clears+reschedules), so no double-advance race; mirror the allVoted check in disconnect so a leaving non-voter completes the round.
  - votedCount uses room.votes.size and the host pairs it with players.length (from lobby:update, stable through the game) — didn't need to add a separate playerCount to the payload.
  - Next story US-009: SPLIT_REVEAL shows the A vs B COUNT on the host (e.g. "A: 4 - B: 2") with no identities. The tally already exists: RoomStore.voteTally(code). Surface it through game:state ONLY in SPLIT_REVEAL onward (add e.g. `tally: {A,B}` to the payload, gated so VOTE_1 still leaks nothing), and render it on the host. Keep votes/identities secret.
---

## 2026-06-20 - US-009
- Implemented: SPLIT_REVEAL shows the aggregate A vs B vote split on the host (counts only, never identities). The split stays hidden during VOTE_1 (and any future voting phase) and is hidden again once the reveal is over.
- Files: server/src/game/rooms.ts (new pure `isSplitRevealed(phase)` predicate + `RoomStore.publicSplit(code)` gated reader over the existing voteTally), server/src/game/__tests__/rooms.test.ts (+4 TDD tests, 50 in file / 60 total), server/src/index.ts (gameStatePayload adds `split: rooms.publicSplit(room.code)`), client/src/shared/events.ts (VoteSplit interface + GameStatePayload.split), client/src/host/HostApp.tsx (SPLIT_REVEAL A/B count cards; bound `const split = game.split` for the closure narrowing).
- Verified: typecheck + lint + test (60 passing) + build ALL green. End-to-end vs built prod server (PORT=3994, 10/10): split null in VOTE_1 and mid-vote, votedCount tracked, auto-advance to SPLIT_REVEAL on all-voted exposes {A:1,B:2}, game:state keys exactly {phase,dilemmaCount,dilemmaIndex,phaseExpiresAt,dilemma,votedCount,split} with NO player ids/nicknames in the payload, split null again in DEFENSE. Visual browser check still pending real browser tooling.
- **Learnings for future iterations:**
  - publicSplit reuses voteTally but gates it by phase — kept the internal always-computing tally separate from the gated broadcast reader; secrecy is structural (the per-choice split simply isn't in the payload outside SPLIT_REVEAL).
  - Gated ONLY to SPLIT_REVEAL on purpose (not "onward"): US-011 adds VOTE_2 to the voting phases and re-uses the votes map for a live re-vote, so revealing through later phases would leak a live split. US-012 (PHASE_RESULTS before->after) will surface its own before/after numbers using the US-011 swing computation.
  - Next story US-010: DEFENSE auto-selects 1 defender per side among those who voted that side (skip a side with 0 votes); host shows who's speaking + a 60-90s countdown per turn (turns in sequence); non-defender phones show 'sta parlando {name}'. The server already knows each player's vote (room.votes) — pick defenders server-side and surface only the speaking player's nickname + per-turn timer via game:state; keep individual votes secret (only the chosen defenders' identities become public, which is inherent to the defense).
---

## 2026-06-20 - US-010
- Implemented: DEFENSE now auto-selects one defender per side (from that side's secret voters, skipping a side with 0 votes) and runs a separate timed turn per defender; the host shows who is speaking + side/option + 'Turno X/Y' + the per-turn countdown, and phones show 'Sta parlando {name}' (the current defender's phone shows 'Tocca a te! Difendi {side}: {option}').
- Files: server/src/game/rooms.ts (isDefensePhase predicate; Defender + DefenseState interfaces; Room.defenders/defenseTurnIndex; injectable rng 4th ctor arg; selectDefenders; advancePhase intra-phase turn loop + DEFENSE-entry selection; publicDefense gated reader), server/src/game/__tests__/rooms.test.ts (+10 TDD defense tests, made the US-008 'keeps votes' test loop-robust; 60 in file / 70 total), server/src/index.ts (gameStatePayload adds `defense: rooms.publicDefense(room.code)`), client/src/shared/events.ts (Defender + DefenseState mirror + GameStatePayload.defense), client/src/host/HostApp.tsx (DEFENSE speaker/side/turn view), client/src/player/PlayerApp.tsx (stores own playerId from player:joined; DEFENSE branch: 'tocca a te' for the defender vs 'sta parlando {name}' for others + no-defense fallback).
- Verified: typecheck + lint + test (70 passing) + build ALL green. E2E vs built prod server (PORT=3993, 18/18): defense null outside DEFENSE (VOTE_1/SPLIT_REVEAL/VOTE_2), split A:1/B:2, DEFENSE turn 1/2 side-A speaker == the A-voter, turn 2/2 side-B speaker == a B-voter, speaker carries ONLY {id,nickname,side}, game:state keys exactly {phase,dilemmaCount,dilemmaIndex,phaseExpiresAt,dilemma,votedCount,split,defense}, unanimous vote -> single defender -> straight to VOTE_2. Visual browser check still pending real browser tooling.
- **Learnings for future iterations:**
  - Intra-phase timed sub-turns: keep nextPhase pure (phase-level), loop the turns in advancePhase before the transition, re-arm expiryFor(phase) per turn — the existing index.ts timer wiring reschedules automatically and host force-advance walks turn-by-turn (see Codebase Patterns).
  - PHASE_DURATIONS_MS.DEFENSE is the PER-TURN duration (60s), so two defenders => up to 120s of defense; fine for the 60-90s/turn AC.
  - Next story US-011: VOTE_2 re-vote (default = previous VOTE_1 vote, changeable) + compute per-voter switch vs VOTE_1 + net swing per side. CAUTION: VOTE_1 votes are cleared on the NEXT DILEMMA_REVEAL but VOTE_2 needs to compare against them — DON'T overwrite the VOTE_1 map during VOTE_2; store the round's first-vote snapshot separately (e.g. votes1) before VOTE_2 starts, and add VOTE_2 to isVotingPhase. publicSplit is gated to SPLIT_REVEAL ONLY precisely so a live VOTE_2 re-vote doesn't leak; keep that.
---

## 2026-06-20 - US-011
- Implemented: second secret vote (VOTE_2) from phones + server-side swing computation. In VOTE_2 each phone re-votes A/B with the player's first vote pre-selected as the changeable default; the server snapshots the first vote, accepts the live re-vote, and computes per-voter side switches + net swing toward each side. Swing stays server-side (not broadcast) — US-012 will reveal before->after in PHASE_RESULTS.
- Files: server/src/game/rooms.ts (isVotingPhase += VOTE_2; Room.votes1 snapshot; VoteTally + SwingResult interfaces; advancePhase snapshots votes1 on VOTE_2 entry + clears votes1 on DILEMMA_REVEAL; leave() prunes votes1; private static tally(map) helper; computeSwing), server/src/game/__tests__/rooms.test.ts (+10 TDD tests — see note below re: concurrent commit), server/src/index.ts (early-advance gated to VOTE_1 only in player:vote + disconnect), client/src/player/PlayerApp.tsx (voting view matches VOTE_1||VOTE_2 + second-vote copy), client/src/host/HostApp.tsx (VOTE_2 'si vota di nuovo' hint).
- Verified: typecheck + lint + test (85 passing: 70 rooms / 14 deck / 1 smoke) + build ALL green. TDD: wrote the 10 US-011 tests first, watched 9 fail (computeSwing missing + VOTE_2 rejected as NOT_VOTING_PHASE), implemented, watched all 70 rooms tests pass. No browser tooling in this env — E2E socket check + visual browser check still pending.
- **Learnings for future iterations:**
  - Swing design: keep `votes` as the live current map and snapshot `votes1` on VOTE_2 entry without clearing `votes` — that makes "default = previous vote" automatic server-side and `computeSwing` a trivial votes1-vs-votes diff. The cost is that `allVoted` is true at VOTE_2 entry, so early-advance MUST be gated to VOTE_1 only (else the first re-voter ends VOTE_2 for everyone). See Codebase Patterns.
  - CONCURRENCY: a parallel loop committed `dae1fde` ('feat: tag dilemmas by register...') via `git add -A` mid-iteration and SWEPT this story's uncommitted rooms.test.ts into ITS commit (also added a now-required `register` field to my fixture + made the deck tests transiently red until that commit tagged dilemmas.json). So the US-011 tests live in dae1fde and only the impl is in the US-011 commit. Commit with EXPLICIT paths, promptly, and never `git add -A`. (Recorded as a Codebase Pattern.)
  - Next story US-012: PHASE_RESULTS shows before->after on the host (e.g. 'A: 4->2, B: 2->4') + how many changed their mind, and scores defenders (+1 'convinzione' per vote their side gained after the defense; 'provocatore' if they defended the split-minority side and gained votes). The numbers are already computed: reuse `RoomStore.computeSwing(code)` (first/second/switched/netSwing). Add a phase-gated `publicResults(code)` reader (mirror publicSplit/publicDefense: only in PHASE_RESULTS) surfaced via gameStatePayload, and a per-player score map on the Room that accumulates across dilemmas (US-013). Unit-test the defender scoring.
---
